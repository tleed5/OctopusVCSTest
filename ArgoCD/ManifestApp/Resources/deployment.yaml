apiVersion: apps/v1
kind: Deployment
metadata:
  name: "nginx-Development"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: "nginx-Development"
  template:
    metadata:
      labels:
        app: "nginx-Development"
    spec:
      containers:
      - name: nginx-container
        image: nginx:1.29.4
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:
            cpu: "250m"
            memory: "256Mi"