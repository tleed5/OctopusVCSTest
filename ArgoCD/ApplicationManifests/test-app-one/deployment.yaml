apiVersion: apps/v1
kind: Deployment
metadata:
  name: "test-app-one-deployment"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: "test-app-one"
  template:
    metadata:
      labels:
        app: "test-app-one"
    spec:
      containers:
      - name: nginx-container
        image: nginx:1.30
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:
            cpu: "250m"
            memory: "256Mi"
